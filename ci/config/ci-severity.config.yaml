apiVersion: mycodexvantaos.io/v1
kind: CiSeverityConfig
metadata:
  version: "1.0.0"
  name: ci-severity-config
spec:
  severityLevels:
    - critical
    - high
    - medium
    - low
  blockingLevels:
    - critical
    - high
