{
  "name": "@mycodexvantaos/advanced-integration",
  "version": "2.0.0",
  "description": "Complete Advanced Integration Package - All Systems",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest",
    "lint": "eslint src/**/*.ts",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.0",
    "ioredis": "^5.0.0",
    "crypto": "^1.0.0"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "typescript": "^5.0.0",
    "eslint": "^8.0.0"
  }
}
