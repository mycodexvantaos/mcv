name: Cache Management

on:
  schedule:
    # Run weekly to refresh caches
    - cron: '0 0 * * 0'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  warm-cache:
    name: 'Warm Cache'
    runs-on: ubuntu-latest
    timeout-minutes: 30

    strategy:
      matrix:
        node-version: ['22']

    steps:
      - uses: actions/checkout@34e114876b0b

      - uses: pnpm/action-setup@b906affcce14
        with:
          version: 9

      - uses: actions/setup-node@49933ea5288c
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build
        run: pnpm run build || echo "Build not yet configured"

      - name: Cache Next.js build
        uses: actions/cache/save@0057852bfaa8
        if: success()
        with:
          path: .next/cache
          key: nextjs-${{ runner.os }}-node${{ matrix.node-version }}-${{ hashFiles('**/pnpm-lock.yaml') }}-${{ github.sha }}
