name: Navigation Validation

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  navigation-validation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: echo "Running navigation-validation"
