id: urn:mycodexvantaos:capability:config-sync
name: Config Sync
version: 1.0.0
description: Configuration synchronization capabilities
status: stable
governance_tier: 1
contract:
  interfaces:
  - initialize
  - execute
  - cleanup
  - validate
  requirements:
  - local-first: true
  - cloud-agnostic: true
  - contract-first: true
  - governance-enforced: true
provider_manifests:
- native: urn:mycodexvantaos:capability:config-sync:provider-native
- external: urn:mycodexvantaos:capability:config-sync:provider-external
