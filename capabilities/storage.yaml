# MyCodeXvantaOS Capability Declaration

id: urn:mycodexvantaos:capability:storage
name: Storage
version: 1.0.0
description: Object storage capabilities
status: stable
governance_tier: 1

contract:
  interfaces:
    - initialize
    - execute
    - cleanup
    - validate

  requirements:
    - local-first: true
    - cloud-agnostic: true
    - contract-first: true
    - governance-enforced: true

provider_manifests:
  - native: 'urn:mycodexvantaos:capability:storage:provider-native'
  - external: 'urn:mycodexvantaos:capability:storage:provider-external'
