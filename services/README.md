# Services

Service implementations for MyCodexVantaOS.

Each subdirectory contains the runtime implementation for a deployable service.
Service manifests are located in `modules/<service-id>/module-manifest.yaml`.

## Structure

```text
services/<service-id>/
├── README.md
├── src/
│   └── .gitkeep
└── tests/
    └── .gitkeep
```
