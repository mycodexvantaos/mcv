{
  "name": "mycodexvantaos-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "MyCodexVantaOS API Gateway — request router for all platform services",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "http-proxy": "^1.18.1"
  }
}
