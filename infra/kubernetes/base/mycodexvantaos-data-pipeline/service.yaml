apiVersion: v1
kind: Service
metadata:
  name: mycodexvantaos-data-pipeline
  labels:
    app.kubernetes.io/name: mycodexvantaos-data-pipeline
spec:
  selector:
    app.kubernetes.io/name: mycodexvantaos-data-pipeline
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
