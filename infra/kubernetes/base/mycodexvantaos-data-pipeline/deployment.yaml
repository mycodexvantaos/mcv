apiVersion: apps/v1
kind: Deployment
metadata:
  name: mycodexvantaos-data-pipeline
  labels:
    app.kubernetes.io/name: mycodexvantaos-data-pipeline
    app.kubernetes.io/managed-by: mycodexvantaos
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mycodexvantaos-data-pipeline
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mycodexvantaos-data-pipeline
    spec:
      containers:
      - name: mycodexvantaos-data-pipeline
        image: ghcr.io/mycodexvantaos/mycodexvantaos-data-pipeline:latest
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: mycodexvantaos-data-pipeline
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /healthz
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 30
