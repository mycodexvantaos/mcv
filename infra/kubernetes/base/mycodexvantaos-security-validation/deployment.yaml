apiVersion: apps/v1
kind: Deployment
metadata:
  name: mycodexvantaos-security-validation
  labels:
    app.kubernetes.io/name: mycodexvantaos-security-validation
    app.kubernetes.io/managed-by: mycodexvantaos
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mycodexvantaos-security-validation
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mycodexvantaos-security-validation
    spec:
      containers:
      - name: mycodexvantaos-security-validation
        image: ghcr.io/mycodexvantaos/mycodexvantaos-security-validation:latest
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: mycodexvantaos-security-validation
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /healthz
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 30
