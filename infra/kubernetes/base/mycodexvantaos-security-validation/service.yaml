apiVersion: v1
kind: Service
metadata:
  name: mycodexvantaos-security-validation
  labels:
    app.kubernetes.io/name: mycodexvantaos-security-validation
spec:
  selector:
    app.kubernetes.io/name: mycodexvantaos-security-validation
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
