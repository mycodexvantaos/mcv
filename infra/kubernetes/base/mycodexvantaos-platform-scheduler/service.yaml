apiVersion: v1
kind: Service
metadata:
  name: mycodexvantaos-platform-scheduler
  labels:
    app.kubernetes.io/name: mycodexvantaos-platform-scheduler
spec:
  selector:
    app.kubernetes.io/name: mycodexvantaos-platform-scheduler
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
