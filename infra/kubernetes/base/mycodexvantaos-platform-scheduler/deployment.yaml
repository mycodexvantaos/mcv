apiVersion: apps/v1
kind: Deployment
metadata:
  name: mycodexvantaos-platform-scheduler
  labels:
    app.kubernetes.io/name: mycodexvantaos-platform-scheduler
    app.kubernetes.io/managed-by: mycodexvantaos
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mycodexvantaos-platform-scheduler
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mycodexvantaos-platform-scheduler
    spec:
      containers:
      - name: mycodexvantaos-platform-scheduler
        image: ghcr.io/mycodexvantaos/mycodexvantaos-platform-scheduler:latest
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: mycodexvantaos-platform-scheduler
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /healthz
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 30
