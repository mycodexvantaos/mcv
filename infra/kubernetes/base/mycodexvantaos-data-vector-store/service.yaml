apiVersion: v1
kind: Service
metadata:
  name: mycodexvantaos-data-vector-store
  labels:
    app.kubernetes.io/name: mycodexvantaos-data-vector-store
spec:
  selector:
    app.kubernetes.io/name: mycodexvantaos-data-vector-store
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
