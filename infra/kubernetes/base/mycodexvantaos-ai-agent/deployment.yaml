apiVersion: apps/v1
kind: Deployment
metadata:
  name: mycodexvantaos-ai-agent
  labels:
    app.kubernetes.io/name: mycodexvantaos-ai-agent
    app.kubernetes.io/managed-by: mycodexvantaos
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mycodexvantaos-ai-agent
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mycodexvantaos-ai-agent
    spec:
      containers:
      - name: mycodexvantaos-ai-agent
        image: ghcr.io/mycodexvantaos/mycodexvantaos-ai-agent:latest
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: mycodexvantaos-ai-agent
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /healthz
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 30
