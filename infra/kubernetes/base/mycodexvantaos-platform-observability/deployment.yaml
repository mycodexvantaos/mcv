apiVersion: apps/v1
kind: Deployment
metadata:
  name: mycodexvantaos-platform-observability
  labels:
    app.kubernetes.io/name: mycodexvantaos-platform-observability
    app.kubernetes.io/managed-by: mycodexvantaos
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mycodexvantaos-platform-observability
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mycodexvantaos-platform-observability
    spec:
      containers:
      - name: mycodexvantaos-platform-observability
        image: ghcr.io/mycodexvantaos/mycodexvantaos-platform-observability:latest
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: mycodexvantaos-platform-observability
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /healthz
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 30
