apiVersion: v1
kind: Service
metadata:
  name: mycodexvantaos-platform-validation
  labels:
    app: mycodexvantaos-platform-validation
spec:
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: mycodexvantaos-platform-validation
