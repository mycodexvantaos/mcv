apiVersion: apps/v1
kind: Deployment
metadata:
  name: mycodexvantaos-core-gateway
  labels:
    app.kubernetes.io/name: mycodexvantaos-core-gateway
    app.kubernetes.io/managed-by: mycodexvantaos
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mycodexvantaos-core-gateway
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mycodexvantaos-core-gateway
    spec:
      containers:
      - name: mycodexvantaos-core-gateway
        image: ghcr.io/mycodexvantaos/mycodexvantaos-core-gateway:latest
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: mycodexvantaos-core-gateway
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /healthz
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 30
