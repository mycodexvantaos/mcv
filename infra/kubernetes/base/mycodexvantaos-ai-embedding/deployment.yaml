apiVersion: apps/v1
kind: Deployment
metadata:
  name: mycodexvantaos-ai-embedding
  labels:
    app.kubernetes.io/name: mycodexvantaos-ai-embedding
    app.kubernetes.io/managed-by: mycodexvantaos
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mycodexvantaos-ai-embedding
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mycodexvantaos-ai-embedding
    spec:
      containers:
      - name: mycodexvantaos-ai-embedding
        image: ghcr.io/mycodexvantaos/mycodexvantaos-ai-embedding:latest
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: mycodexvantaos-ai-embedding
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /healthz
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 30
