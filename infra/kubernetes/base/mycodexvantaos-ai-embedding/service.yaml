apiVersion: v1
kind: Service
metadata:
  name: mycodexvantaos-ai-embedding
  labels:
    app.kubernetes.io/name: mycodexvantaos-ai-embedding
spec:
  selector:
    app.kubernetes.io/name: mycodexvantaos-ai-embedding
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
