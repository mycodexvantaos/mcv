apiVersion: apps/v1
kind: Deployment
metadata:
  name: mycodexvantaos-security-secrets
  labels:
    app.kubernetes.io/name: mycodexvantaos-security-secrets
    app.kubernetes.io/managed-by: mycodexvantaos
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mycodexvantaos-security-secrets
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mycodexvantaos-security-secrets
    spec:
      containers:
      - name: mycodexvantaos-security-secrets
        image: ghcr.io/mycodexvantaos/mycodexvantaos-security-secrets:latest
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: mycodexvantaos-security-secrets
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /healthz
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 30
