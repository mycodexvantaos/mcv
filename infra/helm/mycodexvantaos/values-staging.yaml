# infra/helm/mycodexvantaos/values-staging.yaml
# Environment-specific overrides for staging.
# 12.2 — 'dev'/'staging'/'prod' appears ONLY in this file and namespace names.
global:
  imageTag: 'latest'

services:
  mycodexvantaos-core-kernel:
    replicas: 2
  mycodexvantaos-ai-embedding:
    replicas: 2
  mycodexvantaos-ai-llm:
    replicas: 2
