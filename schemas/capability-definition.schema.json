{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://mycodexvantaos.org/schemas/capability-definition.schema.json",
  "title": "MyCodeXvantaOS Capability Definition",
  "description": "Schema for entries in governance/capability-set.yaml. Based on naming-spec-v1.md 5.5.",
  "type": "object",
  "required": ["id", "description", "category"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "enum": [
        "database",
        "storage",
        "auth",
        "queue",
        "state-store",
        "secrets",
        "repo",
        "deploy",
        "validation",
        "security",
        "observability",
        "notification",
        "scheduler",
        "vector-store",
        "embedding",
        "llm",
        "graph",
        "cache",
        "search"
      ],
      "description": "5.5 — Canonical capability-id. Vendor names are forbidden."
    },
    "description": { "type": "string", "minLength": 10 },
    "category": {
      "type": "string",
      "enum": ["data", "infra", "ai", "platform", "security", "governance"]
    },
    "allowedProviderPattern": {
      "type": "string",
      "description": "Regex pattern that provider names for this capability must match."
    },
    "stable": {
      "type": "boolean",
      "description": "Whether this capability is stable in the canonical set."
    }
  }
}
