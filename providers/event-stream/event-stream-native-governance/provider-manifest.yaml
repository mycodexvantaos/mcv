# providers/event-stream/event-stream-native-governance/provider-manifest.yaml
# 8.1 — provider instance: event-stream-native-governance
# 8.2 — capability (event-stream) precedes provider name (native-governance)
apiVersion: mycodexvantaos.org/v1
kind: Provider
metadata:
  name: event-stream-native-governance
  capability: event-stream
  labels:
    mycodexvantaos.org/managed-by: naming-spec-v1
    mycodexvantaos.org/specRef: '8.1'
spec:
  providerName: native-governance
  isNative: true
  lifecycle:
    stage: stable
  envVarPrefix: MYCODEXVANTAOS_EVENT_STREAM
  configSchemaRef: ./config.schema.json
  description: 'Native governance event stream provider for mandatory closed-loop compliance verification, audit trail propagation, and Merkle-anchored event integrity.'
