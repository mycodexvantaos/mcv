# providers/scheduler/scheduler-temporal/provider-manifest.yaml
# 8.1 — provider instance: scheduler-temporal
# 8.2 — capability (scheduler) precedes provider name (temporal)
apiVersion: mycodexvantaos.org/v1
kind: Provider
metadata:
  name: scheduler-temporal
  capability: scheduler
  labels:
    mycodexvantaos.org/managed-by: naming-spec-v1
    mycodexvantaos.org/specRef: '8.1'
spec:
  providerName: temporal
  isNative: false
  lifecycle:
    stage: stable
  envVarPrefix: MYCODEXVANTAOS_SCHEDULER
  configSchemaRef: ./config.schema.json
  description: 'Temporal workflow orchestration provider.'
