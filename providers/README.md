# Providers

Provider implementations for MyCodexVantaOS.

Provider instance naming MUST be capability-first: `<capability>-<provider>`

## Canonical Capabilities

database, storage, auth, queue, cache, vector-store, embedding, llm, graph, search,
secrets, observability, quantum-runtime, quantum-simulator, quantum-processor,
quantum-circuit, quantum-observability
